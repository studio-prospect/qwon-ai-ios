# QWON — M3 Gate 2 Artifact Record Template

**Last updated:** 2026-06-06
**Status:** **Template only** — no artifact finalized, no Q-A-06/Q-A-07 answers recorded, no Gate 2 Ready sign-off, no M3 implementation approval, no Build `4` approval.
**Purpose:** Provide the exact form Release Engineering should fill after the final QWON-hosted GGUF object exists.

Related: [Gate 2 artifact finalization runbook](./qwon_m3_gate2_artifact_finalization_runbook.md) · [Gate answer intake ledger](./qwon_m3_gate_answer_intake.md) · [Hosting + checksum memo](./qwon_m3_model_hosting_checksum_memo.md)

---

## Instructions

Copy this template into the release evidence location, fill every field, then use that completed record as the answer source for **Q-A-06** and **Q-A-07** in the intake ledger.

Do not commit the GGUF file, downloaded blob, raw command logs, screenshots, IPA, archive, or any secret storage URL credential.

---

## Artifact record

| Field | Value |
| --- | --- |
| **Record date** | `YYYY-MM-DD` |
| **Product decision owner** | `Product owner / role` |
| **Release Engineering owner** | `RE owner / role` |
| **Approved artifact direction** | `QWON-hosted mirror of bartowski Qwen2.5-0.5B-Instruct Q4_K_M GGUF` or approved replacement |
| **Source artifact URL / revision** | `source URL + revision / commit / version` |
| **QWON hosted object identity** | `bucket + object key + version ID / etag / immutable revision` |
| **Product URL class** | `QWON-owned CDN/object storage` |
| **On-device filename** | `prexus-local-mvp.gguf` |
| **Exact byte size** | `integer bytes` |
| **SHA-256** | `64 lowercase hex chars` |
| **Generated by** | `operator + machine + command summary` |
| **Verification repeat** | `same values repeated from local staged object and/or downloaded hosted object` |
| **Evidence storage path** | `ops path outside git` |

---

## Command evidence summary

```text
shasum -a 256 <final-object-path>
<64 lowercase hex chars>  <final-object-path>

stat -f%z <final-object-path>
<integer bytes>
```

If verifying the hosted object after upload:

```text
downloaded hosted object path:
repeat SHA-256:
repeat byte size:
match result: pass / fail
```

---

## Intake answers derived from this record

Use these exact forms when opening the follow-up intake PR.

| Question ID | Answer source | Answer |
| --- | --- | --- |
| **Q-A-06** | Completed artifact record | Exact expected byte size is `<integer bytes>` for hosted object `<object identity>`. |
| **Q-A-07** | Completed artifact record | SHA-256 is `<64 lowercase hex chars>` for hosted object `<object identity>`. |

Keep Gate 2 **Pending** unless the same PR is explicitly a Ready sign-off PR with Product/Codex approval.

---

## Completion checklist

| Check | Required result |
| --- | --- |
| Hosted object identity recorded | Yes |
| Exact byte size is an integer | Yes |
| SHA-256 is 64 lowercase hex characters | Yes |
| Byte size and SHA-256 refer to the same object identity | Yes |
| Repeat verification performed | Yes |
| GGUF binary kept out of git | Yes |
| Q-A-06/Q-A-07 follow-up PR prepared | Yes |
| M3 spike / Build `4` still unapproved | Yes |

---

## Non-approvals

This template does **not** approve:

- M3 in-app download implementation
- Network fetch / downloader prototype
- Storage schema change
- Gate 2 Ready sign-off
- Build `4`, TestFlight upload, tag, or version bump
- Replacing or renaming `prexus-local-mvp.gguf`
- Silent overwrite of existing USB-placed GGUF files
