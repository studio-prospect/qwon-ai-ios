import Foundation

enum PromptTemplate {
    static let routerSystem = "Classify the task, minimize cloud use, and preserve privacy."
    static let compressionSystem = "Compress the context while preserving task-critical facts."
}
